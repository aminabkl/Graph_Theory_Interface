 <body>
	{% include 'navbar.html' %}
		<h1 class="text-center">KOSARAJU</h1>
		<div class="container">
			<div class="row">
				<div class="form-div">
					<form method="POST">
						<div class="form-group">
							<label for="num_nodes">Number of Nodes:</label>
							<input
								type="number"
								class="form-control"
								name="num_nodes"
								id="num_nodes"
								required
							/>
							<button type="submit" class="submit-btn">Submit</button>
						</div>
						<table class="table table-bordered" id="adj_matrix_table">
							<script>
								$("#num_nodes").on("input", function () {
									const numNodes = $(this).val();
									const adjMatrixTable = $("#adj_matrix_table");
									adjMatrixTable.empty();

									const headerRow = $("<tr>");
									headerRow.append($("<th>"));
									for (let i = 0; i < numNodes; i++) {
										const th = $("<th>").text(i);
										headerRow.append(th);
									}
									adjMatrixTable.append(headerRow);

									for (let i = 0; i < numNodes; i++) {
										const row = $("<tr>");
										const th = $("<th>").text(i);
										row.append(th);
										for (let j = 0; j < numNodes; j++) {
											const td = $("<td>");
											const input = $("<input>").attr({
												type: "number",
												name: `adj_matrix_${i}_${j}`,
												required: true,
											});
											td.append(input);
											row.append(td);
										}
										adjMatrixTable.append(row);
									}
								});
							</script>
						</table>
					</form>
				</div>
			</div>
		</div>
		<div
			id="kosaraju-result"
			{%
			if
			display_result
			%}
			style="display: block"
			{%
			else
			%}
			style="display: none"
			{%
			endif
			%}
		>
			<h2 class="text-center">The strongly connected components :</h2>
			<ul>
				{% for res in kosaraju_result %}
				<li class="result-span">{{ res }}</li>
				{% endfor %}
			</ul>
			<div class="graph-res">
				<div class="aft">
					<h2 class="text-result-aft">Graph:</h2>
					<img
						src="{{ url_for('static', filename='img/kosaraju_graph.png') }}"
						alt="Kosaraju's Graph"
						class="result-img-aft"
					/>
				</div>
			</div>
		</div>
		{% block content %}{% endblock %}
	</body> 
